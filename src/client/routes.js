import Home from './components/Home';
import History from './components/History';

const routes = [
   {
      path: '/history',
      component: History,
   },
   {
      path: '/',
      component: Home,
   },
];

export default routes;
