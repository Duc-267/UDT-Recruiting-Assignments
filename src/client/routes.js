import Home from './Home';
import Calculator from './Calculator';

const routes = [
    {
        path: '/',
        component: Home,
    },
    {
        path: '/calculator',
        component: Calculator,
    },
];

export default routes;
